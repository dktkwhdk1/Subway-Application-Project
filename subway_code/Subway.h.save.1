
#ifndef SUBWAY_H
#define SUBWAY_H

#include <iostream>
#include <fstream>
#include <stack> // 지하철 경로를 출력할때 필요
#include <string> 
using namespace std;

enum color { white, gray }; // 역을 지났는지 안지났는지 판단

class Station // 역에 대한 정보
{
public:
	int num; // 역에 매겨진 숫자
	int time; // 이동시간
	int transfer; // 환승 횟수	
르키는 포인터

	Station() : next(NULL) {}
};
